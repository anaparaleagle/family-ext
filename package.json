{
  "name": "paraleagle-family-ext",
  "version": "0.9.0",
  "private": true,
  "description": "Autofill Forms I-130, I-539 and N-400 on my.uscis.gov, and the ETA-9141 on the DOL FLAG portal, from ParaLeagle family-visa data",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "check:listing": "node tools/check-live-listing.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.0",
    "happy-dom": "^14.12.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "firebase": "^12.11.0"
  }
}
