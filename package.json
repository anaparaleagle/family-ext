{
  "name": "paraleagle-family-ext",
  "version": "0.8.1",
  "private": true,
  "description": "Autofill the guided online Forms I-130, I-539 and N-400 on myUSCIS from ParaLeagle family-visa data",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.0",
    "happy-dom": "^14.12.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "firebase": "^12.11.0"
  }
}
